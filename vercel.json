{
  "build": {
    "env": {
      "JEKYLL_ENV": "production"
    }
  },
  "buildCommand": "gem install bundler && bundle install && bundle exec jekyll build --baseurl \"\"",
  "outputDirectory": "_site",
  "cleanUrls": true
}